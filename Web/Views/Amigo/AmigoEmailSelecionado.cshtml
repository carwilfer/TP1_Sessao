@model IEnumerable<Domain.Amigo>

@{
    ViewData["Title"] = "AmigoEmail";
}

@section scripts {

    @*<script>
        $(document).ready(function () {
            var ids = [];
            $("input[type='hidden']").each(function (i, e) {
                ids.push($(e), val());
            })
            $("#lnkNavAmigos").click(function () {
                location.href = " /Amigo/AmigoEmail?ids=" + ids.join(",");
            })
        })
    </script>*@
}

<h1>AmigoEmail</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sobrenome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sobrenome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    @Html.ActionLink("Details", "Details", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>
<div>
    <a asp-action="AmigoEmail">Ir para a lista de seleção</a>
</div>
